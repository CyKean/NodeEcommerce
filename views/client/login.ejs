<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/output.css">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            background-image: url('/images/luckyme.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed;
        }
    </style>
</head>
<body class="flex items-center justify-center h-screen">
    <div class="absolute inset-0 bg-black opacity-50"></div>

    <div class="relative bg-white bg-opacity-90 backdrop-blur-lg p-10 rounded-lg shadow-lg max-w-md w-full z-10">
        <h2 class="text-center text-yellow-500 font-bold text-4xl mb-8">Login</h2>
        <form action="/login" method="POST">
            <div class="mb-5">
                <label for="name" class="block text-sm font-medium text-gray-800 mb-2">Username</label>
                <input type="text" id="name" name="name" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-yellow-500 focus:border-transparent transition duration-200" placeholder="Enter your name" required>
            </div>
            <div class="mb-8">
                <label for="password" class="block text-sm font-medium text-gray-800 mb-2">Password</label>
                <input type="password" id="password" name="password" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-yellow-500 focus:border-transparent transition duration-200" placeholder="Enter your password" required>
            </div>
            <button type="submit" class="w-full bg-gradient-to-r from-yellow-500 to-orange-600 text-white font-semibold py-2 rounded-lg shadow-lg hover:bg-gradient-to-r hover:from-yellow-600 hover:to-orange-700 transition duration-300">Log In</button>
            <p class="mt-6 text-sm text-gray-600 text-center">
                Don't have an account? <a href="/register" class="text-yellow-600 hover:underline font-semibold">Register</a>
            </p>            
        </form>
    </div>
</body>
</html>
